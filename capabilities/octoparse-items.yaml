naftiko: 1.0.0-alpha2
info:
  label: Octoparse — Items
  description: 'Octoparse — Items. 1 operations. Lead operation: Octoparse updateLoopItems. Self-contained Naftiko capability
    covering one Octoparse business surface.'
  tags:
  - Octoparse
  - Items
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    OCTOPARSE_API_KEY: OCTOPARSE_API_KEY
capability:
  consumes:
  - type: http
    namespace: octoparse-items
    baseUri: https://openapi.octoparse.com
    description: Octoparse — Items business capability. Self-contained, no shared references.
    resources:
    - name: task-updateLoopItems
      path: task/updateLoopItems
      operations:
      - name: post
        method: POST
        description: Octoparse updateLoopItems
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: octoparse-items-rest
    port: 8080
    description: REST adapter for Octoparse — Items. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/task/updateloopitems
      name: task-updateloopitems
      description: REST surface for task-updateLoopItems.
      operations:
      - method: POST
        name: post
        description: Octoparse updateLoopItems
        call: octoparse-items.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: octoparse-items-mcp
    port: 9090
    transport: http
    description: MCP adapter for Octoparse — Items. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: octoparse-updateloopitems
      description: Octoparse updateLoopItems
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: octoparse-items.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
