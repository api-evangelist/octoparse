naftiko: 1.0.0-alpha2
info:
  label: Octoparse — Properties
  description: 'Octoparse — Properties. 1 operations. Lead operation: Octoparse updateActionProperties. Self-contained Naftiko
    capability covering one Octoparse business surface.'
  tags:
  - Octoparse
  - Properties
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    OCTOPARSE_API_KEY: OCTOPARSE_API_KEY
capability:
  consumes:
  - type: http
    namespace: octoparse-properties
    baseUri: https://openapi.octoparse.com
    description: Octoparse — Properties business capability. Self-contained, no shared references.
    resources:
    - name: task-updateActionProperties
      path: task/updateActionProperties
      operations:
      - name: post
        method: POST
        description: Octoparse updateActionProperties
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: octoparse-properties-rest
    port: 8080
    description: REST adapter for Octoparse — Properties. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/task/updateactionproperties
      name: task-updateactionproperties
      description: REST surface for task-updateActionProperties.
      operations:
      - method: POST
        name: post
        description: Octoparse updateActionProperties
        call: octoparse-properties.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: octoparse-properties-mcp
    port: 9090
    transport: http
    description: MCP adapter for Octoparse — Properties. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: octoparse-updateactionproperties
      description: Octoparse updateActionProperties
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: octoparse-properties.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
